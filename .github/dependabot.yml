version: 2
updates:
  # Keep pinned GitHub Actions SHAs current (security patches for the release pipeline).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
  - package-ecosystem: pip
    directory: /
    schedule:
      interval: weekly
