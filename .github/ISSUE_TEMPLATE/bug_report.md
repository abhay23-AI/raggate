---
name: Bug report
about: Something scored or gated wrong
labels: bug
---

**What happened**

**Repro** — the `case`, the `output` your `target.run()` returned, and the score you expected vs got:

```json

```

**Environment**: raggate version (`raggate --version`), Python version, judge mode (heuristic / openai).
